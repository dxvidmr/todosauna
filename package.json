{
  "name": "todosauna-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "E2E smoke tests for Todos a una",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --project=chromium --headed",
    "test:e2e:ci": "playwright test --project=chromium --reporter=line,github"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
